import { DropdownlistDirective } from './dropdownlist.directive';

describe('DropdownlistDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownlistDirective();
    expect(directive).toBeTruthy();
  });
});
